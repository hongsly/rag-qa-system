# RAG Q&A System - Development Requirements
# Includes runtime + build-time + evaluation + testing dependencies
# Install with: pip install -r requirements-dev.txt

# Include all runtime requirements
-r requirements.txt

# Build-time (index creation from PDFs)
pymupdf4llm==0.2.3
arxiv==2.3.1
tqdm==4.67.1

# Evaluation (RAGAS)
ragas==0.3.9
# rapidfuzz - installed as ragas dependency
langchain-ollama==1.0.0

# Development
pytest==9.0.1
#black==23.11.0
flake8==7.3.0
